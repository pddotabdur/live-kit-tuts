[project]
name = "live-kit-tuts"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[google,silero,turn-detector,openai,deepgram]~=1.5",
    "livekit-plugins-noise-cancellation>=0.2",
    "livekit-plugins-faseeh>=0.1",
    "python-dotenv>=1.2.2",
]