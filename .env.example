# ── LiveKit ─────────────────────────────────────────────────────────────────
# On the EC2 box (where agent.py runs in production), use loopback so audio
# publish stays local — this is the fix for "STT works but caller hears nothing".
# LIVEKIT_URL=ws://localhost:7880
#
# On your laptop (where dispatch.py runs), use the EC2 public IP.
LIVEKIT_URL=ws://13.49.62.84:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# ── SIP ─────────────────────────────────────────────────────────────────────
SIP_OUTBOUND_TRUNK_ID=ST_kXAsqyEMzhmg

# ── Per-developer dev loop ──────────────────────────────────────────────────
# Each developer should register their agent under a unique name so calls
# dispatched to outbound-caller-<name> only land on their machine.
# Leave blank in production (defaults to outbound-caller).
# AGENT_NAME=outbound-caller-yourname
# PHONE_NUMBER=+966555209485

# ── External AI services ────────────────────────────────────────────────────
OPENAI_API_KEY=
DEEPGRAM_API_KEY=
# Optional alternates (not required by agent.py as written):
# FASEEH_API_KEY=
# GOOGLE_API_KEY=
# CARTESIA_API_KEY=
# HAMSA_API_KEY=
# ELEVENLABS_API_KEY=
